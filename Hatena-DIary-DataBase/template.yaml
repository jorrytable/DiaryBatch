AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: はてなブログ解析システム

# 1. sam deploy --guided で入力した値を受け取るための設定
Parameters:
  HatenaId:
    Type: String
    Description: "はてなIDを入力してください"
  BlogId:
    Type: String
    Description: "ブログのドメイン（例: example.hatenablog.com）を入力してください"

Resources:
  # フロントエンドからアクセスするAPI Gateway本体（Lambda Authorizerで保護）
  HatenaApi:
    Type: AWS::Serverless::Api
    Properties:
      StageName: Prod
      # 注意: BinaryMediaTypesは設定しないこと。過去に設定したところ、
      # CORSプリフライト(OPTIONSのMock統合)の応答まで巻き込まれて500エラーになった
      # （"*/*"はもちろん、"application/gzip"のみに絞ってもOPTIONSは直らなかった）。
      # そのためSearchFunctionのgzip圧縮はAPI Gateway経由のバイナリ変換に頼らず、
      # base64文字列としてそのまま返しフロント側で展開する方式にしている。
      Cors:
        # AllowOriginは現時点ではVercelドメイン未確定のため暫定的に全許可。
        # デプロイ後、実際のVercelドメインに絞り込むこと。
        AllowOrigin: "'*'"
        AllowHeaders: "'Content-Type,Authorization'"
        AllowMethods: "'GET,OPTIONS'"
      Auth:
        DefaultAuthorizer: SharedSecretAuthorizer
        # falseにしないとCORSプリフライト(OPTIONS)にもAuthorizerが適用され、
        # Authorizationヘッダーを持たないプリフライトが401になりCORSエラーになる
        AddDefaultAuthorizerToCorsPreflight: false
        Authorizers:
          SharedSecretAuthorizer:
            FunctionArn: !GetAtt AuthorizerFunction.Arn
            Identity:
              Header: Authorization
              ReauthorizeEvery: 300

  # 合言葉(Authorizationヘッダー)をSSMの秘密値と照合するLambda Authorizer
  AuthorizerFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: src/backend/
      Handler: api.authorizer.lambda_handler
      Runtime: python3.11
      Environment:
        Variables:
          SSM_PARAM_NAME: "/hatena-site/access-token"
      Policies:
        - Statement:
            - Effect: Allow
              Action:
                - ssm:GetParameter
              Resource: !Sub "arn:aws:ssm:${AWS::Region}:${AWS::AccountId}:parameter/hatena-site/access-token"

  # データを保存するテーブル
  ReviewTable:
    Type: AWS::DynamoDB::Table
    Properties:
      TableName: HatenaBlogReviews
      BillingMode: PAY_PER_REQUEST
      AttributeDefinitions:
        - AttributeName: id
          AttributeType: S
        - AttributeName: review_date
          AttributeType: S
        - AttributeName: data_type
          AttributeType: S
      KeySchema:
        - AttributeName: id
          KeyType: HASH
      GlobalSecondaryIndexes:
        # DynamoDBは既存GSIのキースキーマをその場で変更できないため、
        # 旧DateIndex（review_dateのみHASH）とは別名のGSIとして新設する
        - IndexName: FeedIndex
          KeySchema:
            - AttributeName: data_type
              KeyType: HASH
            - AttributeName: review_date
              KeyType: RANGE
          Projection:
            ProjectionType: ALL

  # URL単位のtitle/embed情報キャッシュ（ReviewTableの日次全削除の影響を受けず永続する）
  UrlMetadataCacheTable:
    Type: AWS::DynamoDB::Table
    Properties:
      TableName: HatenaUrlMetadataCache
      BillingMode: PAY_PER_REQUEST
      AttributeDefinitions:
        - AttributeName: url
          AttributeType: S
      KeySchema:
        - AttributeName: url
          KeyType: HASH

  # プログラム本体
  BatchFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: src/backend/
      Handler: batch.app.lambda_handler
      Runtime: python3.11
      # 過去ページを辿るページネーション取得に対応するため、既定の3秒からLambdaの上限値まで延長
      Timeout: 900
      # 2. プログラム(app.py)が読み取る環境変数の設定
      Environment:
        Variables:
          REVIEW_TABLE_NAME: !Ref ReviewTable
          URL_CACHE_TABLE_NAME: !Ref UrlMetadataCacheTable
          SSM_PARAM_NAME: "/hatena-batch/api_key"
          YOUTUBE_API_KEY_PARAM_NAME: "/hatena-batch/youtube-api-key"
          HATENA_ID: !Ref HatenaId
          BLOG_ID: !Ref BlogId
      # 3. 権限の設定
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref ReviewTable
        - DynamoDBCrudPolicy:
            TableName: !Ref UrlMetadataCacheTable
        - Statement:
            - Effect: Allow
              Action:
                - ssm:GetParameter
              Resource: !Sub "arn:aws:ssm:${AWS::Region}:${AWS::AccountId}:parameter/hatena-batch/api_key"
        - Statement:
            - Effect: Allow
              Action:
                - ssm:GetParameter
              Resource: !Sub "arn:aws:ssm:${AWS::Region}:${AWS::AccountId}:parameter/hatena-batch/youtube-api-key"
      Events:
        # 毎日 JST 3:00（UTC 18:00）に自動実行
        DailySchedule:
          Type: Schedule
          Properties:
            Schedule: "cron(0 18 * * ? *)"

  # データを検索・取得するAPI用プログラム
  SearchFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: src/backend/
      Handler: api.search.lambda_handler # apiフォルダのsearch.pyを呼び出す
      Runtime: python3.11
      # 全件取得のためQueryを複数回ページングする可能性があるため、既定の3秒から延長
      Timeout: 30
      Environment:
        Variables:
          REVIEW_TABLE_NAME: !Ref ReviewTable
      Policies:
        - DynamoDBReadPolicy: # 読み取り専用権限
            TableName: !Ref ReviewTable
      Events:
        # ここがAPI Gatewayの設定（認証付きのHatenaApiに紐付ける）
        GetReviews:
          Type: Api
          Properties:
            RestApiId: !Ref HatenaApi
            Path: /reviews
            Method: get

# 最後にURLを表示するための設定（ファイルの末尾に追加）
Outputs:
  SearchApiUrl:
    Description: "APIのエンドポイントURLです"
    Value: !Sub "https://${HatenaApi}.execute-api.${AWS::Region}.amazonaws.com/Prod/reviews"